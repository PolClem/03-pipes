import { CapitlizadoPipe } from './capitlizado.pipe';

describe('CapitlizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitlizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
